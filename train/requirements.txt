trl>=0.12.0
# TRL 0.2x: grpo_trainer import chain loads mergekit_utils — without mergekit, Colab
# fails with: ModuleNotFoundError: No module named 'mergekit'
mergekit
transformers>=4.46.0
peft>=0.12.0
accelerate>=0.34.0
datasets>=2.19.0
wandb>=0.17.0
requests>=2.32.0
matplotlib>=3.8.0
uvicorn>=0.30.0
# Unsloth for 4-bit QLoRA training (MR-3 — required by hackathon spec)
# In Colab, install via: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
unsloth
# env server deps (train_minimal.py imports server.calibration_grader + server.claim_generator)
openenv-core>=0.2.3
