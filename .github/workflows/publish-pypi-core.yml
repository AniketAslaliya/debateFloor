name: publish-pypi-core (disabled)
# Disabled: this workflow is from the OpenEnv monorepo and does not apply
# to this hackathon submission repo. All CI is handled by validate.yml.
on:
  workflow_dispatch:
    inputs:
      _note:
        description: "Disabled — see validate.yml"
        required: false
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled for the hackathon submission repo."
